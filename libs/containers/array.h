#pragma once
#include <stdbool.h>

#define ARRAY_SIZE(array) (sizeof(array) / sizeof(array[0]))

//#define IN_ARRAY(array, type) \
//	( \
//		for(int i = 0; i < ARRAY_SIZE(array); ++i) { \
//			if( \
//		} \
//	)
